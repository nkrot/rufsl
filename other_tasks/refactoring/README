Задание
=======

Необходимо произвести рефакторинг скрипта assign_properties_messy, разработанного группой индийских профессионалов.

Входной файл с данными: ??????? взять из crf_fa
Ожидаемый выход: ?????

Примерный результат рефакторинга: assign_properties

TODO:
дать два варианта входа, 
1) слово \t тег
2) слово \t какие-нибудь фичи \t тег

скрипт должен работать с двумя входами

дать описание фич!

класс со словарем SynLDB (synldb.rb) рефакторить не надо

совет:
1. уменьшить файл словаря, чтобы быстрее  работало. потом, когда вы поймете, что и как ускорить, можно подключить полный словарь заново
2. описывайте словами то, что вы делаете, например: 
   вынес повторяющийся код в отдельный метод
   вынес код в отдельный метод, потому что у него четкая задача, single responsibility
   переименовал переменные
