-*- coding: windows-1251; -*-

Задание 1
========

Разработайте скрипт (find_vb_multivbd.rb), который находит в словаре просао1 глаголы, имеющие больше одной формы VBD, и выведит их в следующем формате

	MISSPELL	VB	-->	VBD	MISSPELLT	MISSPELLED

	(т.е. 6 полей, разделенных табуляцией)

Используйте DicTester как источник данных (DicTester --all).

В решении удобно (но необязательно) использовать массивы.

Ожидаемый выход:

	find_vb_multivbd.out

Задание 2
========

Разработайте скрипт (find_vb_incomplete_pdg.rb), который находит в словаре просао глаголы, имеющие неполную парадигму. Чтобы парадигма считалась полной, каждая форма VB должна иметь конвертации во все следующие теги: VBZ, VBG, VBD, VBN.

Примерный формат вывода:

	форма VB \t список недостающих тегов через табуляцию

например:

	RECRUDESCE	VBZ	VBG	VBD	VBN
	CYCLONE	VBD	VBN

Ожидаемый выход:

	find_vb_incomplete_pdg.out

Calm down: нехватка конвертаций не является багом, так как возможно нужные конвертации правильно конструируются по формальным признакам.


